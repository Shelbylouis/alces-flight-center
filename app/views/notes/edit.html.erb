<%= render 'partials/tabs', activate: :documents do %>
    <%= render 'form',
               button_text: 'Save document',
               policy: policy(note).update?,
               can_set_visibility: policy(Note).set_visibility?
    %>
<% end %>
