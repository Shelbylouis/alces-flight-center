<p><%= t(".opening") %></p>

<p>
  <%= link_to t(".link_text"),
    reset_password_complete_url(
      user_id: @user.id,
      token: @user.confirmation_token.html_safe)
    %>
</p>

<p><%= raw t(".closing") %></p>
